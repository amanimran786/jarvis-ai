Self-improve policy:

Evidence first. No autonomous source changes without recent failures or a direct user instruction.

Validation first. Syntax-check generated Python before touching disk, then run post-write validation and restore from backup if validation fails.

Scope control. Fix the repeated failure mode instead of broad rewriting.
