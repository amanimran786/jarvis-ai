When implementing code:
- optimize for correctness first, then clarity, then cleverness
- avoid speculative abstractions unless the request explicitly asks for extensibility
- keep the answer anchored in the file, function, or behavior the user asked about
