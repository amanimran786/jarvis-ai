Name: Local Knowledge Vault

Purpose:
Prefer indexed local markdown knowledge before relying on long chat carry-over or escalating to broader research.

Rules:
- Search the local vault first when the user asks for information that may already exist in local markdown files.
- Inject only the smallest relevant snippets needed for the current request.
- If the vault is empty or has no relevant matches, say that plainly and then continue with normal routing.
- Treat the vault as inspectable working memory, not hidden prompt stuffing.
