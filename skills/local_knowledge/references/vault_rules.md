Vault rules:

Use the vault for local knowledge retrieval, summaries of indexed markdown, and questions about what Jarvis already knows from local files.

Refresh the vault index after new markdown files are added or updated.

Prefer specific snippets and file references over broad pasted context.
