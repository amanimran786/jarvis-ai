# Jarvis AI — Commit Staging Plan (2026-06-06)

Generated by commit-staging audit agent. 1588 passed, 0 failed baseline.

## Files to EXCLUDE
- `workspace/` — auto-generated security scan artifacts (added to .gitignore)

## Commit Order

### Commit 1: Researcher agent + Manager security gating
```
agents/researcher.py (new)
core/manager.py
tests/test_researcher_agent.py (new)
tests/test_cloud_research_gate.py (new)
```

### Commit 2: Generic agent worker dispatcher
```
agents/agent_worker.py (new)
tests/test_agent_workers.py (new)
```
Depends on: Commit 1

### Commit 3: ADE — Autonomous Dev Environment
```
ade/session.py
ade/cli.py
tests/test_ade.py
```

### Commit 4: Event bus approval endpoints + work-order preview
```
infra/event_bus.py
core/work_order.py (new)
tests/test_event_bus.py
tests/test_work_order.py (new)
```
Depends on: Commit 1

### Commit 5: API — agent ID normalization + /manager/work-order endpoint
```
api.py
tests/test_jarvis_regression_suite.py (agent_run_accepts_task_runtime_hyphenated_agent_ids)
tests/test_agent_dispatch_integration.py (unskip work_order test)
```
Depends on: Commits 1-4

### Commit 6: Docker — six new worker services
```
docker-compose.yml
```
Depends on: Commits 2-3

### Commit 7: Test infrastructure — sys.modules contamination fixes
```
tests/conftest.py (new)
tests/test_agent_dispatch.py
tests/test_backend_engineer.py
tests/test_manager.py
tests/test_message_intent_parsing.py
tests/test_jarvis_executor.py
tests/test_jarvis_new_fastpaths.py
tests/test_cocoindex.py
tests/test_memory.py
tests/test_mlx_training.py
tests/test_overnight_trainer.py
tests/test_messages_contacts.py
tests/test_persistent_jarvis_v1.py
```

### Commit 8: Local runtime — CocoIndex chunking + finetune scheduler
```
local_runtime/local_cocoindex.py
local_runtime/local_finetune_scheduler.py
```

### Commit 9: Training dashboard + memory test isolation
```
training/dashboard_generator.py
tests/test_manager.py (autouse fixture additions)
tests/test_memory.py (autouse fixture additions)
```
Depends on: Commit 7

### Commit 10: UI — ApprovalPanel + WorkOrderPanel
```
ui.py
```
Depends on: Commits 4-5

### Commit 11: Config, CLI, Jarvis.spec, docs
```
jarvis_cli.py
agent_dispatch.py
Jarvis.spec
CODEX_CLAUDE_COORDINATION.md (new)
CODEX_HANDOFF.md
.gitignore
```

### Commit 12: Vault updates
```
vault/wiki/brain/30 Preferences.md
vault/wiki/brain/90 Task Hub.md
vault/indexes/index.json
```

---
# Phase 2 — Boris Architecture (2026-06-06, Claude Session 5+6)

### Commit 13: Security must-fixes
```
infra/event_bus.py (threat screen + approval auth)
tools/security/hackingtool_adapter.py (fail-closed token)
core/work_order.py (cloud_research sanitize comment)
tests/test_event_bus.py (auth regression tests)
tests/test_hackingtool_adapter.py (token fixtures)
```

### Commit 14: Email digest fast-path + briefing agents
```
router.py (_is_email_digest_query expansion + digest fast-path)
jarvis_agents.py (_agent_calendar_upcoming + _agent_pending_alerts)
tests/test_email_digest_briefing.py (new, 25 tests)
tests/test_email_reply_reminder.py (new, 21 tests + isolation fix)
```

### Commit 15: Phase 2 domain agents
```
agents/career_agent.py (new)
agents/automation_engineer.py (new)
agents/ai_safety_agent.py (new)
config.py (3 new AGENT_ROSTER entries)
tests/test_phase2_agents.py (new, 38 tests)
```

### Commit 16: JARVIS.md system + infra
```
JARVIS.md (new — system intelligence regression file)
STAGING_PLAN.md (updated)
infra/jarvis_md.py (new — automated read/append)
infra/checkpointer.py (new — session teleportation)
tests/test_checkpointer.py (new, 19 tests)
```

### Commit 17: ADE fleet orchestration + post-tool hooks
```
ade/loop.py (post-tool hook + JARVIS.md integration)
ade/fleet.py (new — parallel worker fleet management)
Jarvis.spec (all new hiddenimports)
```

---
# Phase 3 — DRAFT extension by Claude session C, 2026-06-10 19:10 (coordinator; see ~/CLAUDE_SESSIONS.md)
# Covers dirty/untracked files not in Commits 13–17. Owner sessions: review groupings before staging.

### Commit 18: Security gate cloud fallback (opt-in gated)
```
agents/security_reviewer.py (Gemini → GPT-4o-mini → Haiku fallback when local LLM returns empty;
  gated: requires JARVIS_ALLOW_CLOUD_SECURITY_REVIEW truthy AND not open-source mode — added 2026-06-10 19:42 per B's review verdict)
tests/test_security_reviewer_cloud_gate.py (new, 7 tests: gate denial/allow, no provider client without opt-in, review() fails closed)
```
B's must-fix (a) opt-in gate and (b) dedicated test: DONE (session C). Still open per B: (c) root-cause why local LLM returns empty (suspected Ollama memory pressure) — owner call whether that blocks staging.

### Commit 19: Google OAuth reauth helper
```
google_services.py (reauthorize_google/clear_google_token + --reauth/--status CLI)
tests/test_google_oauth_reauth.py (new)
```

### Commit 20: Agent runtime reliability — verifier, lessons, tool-loop history
```
task_runtime.py (lesson tail-budget, inherited-transcript verification, verdict JSONL log, compacted history blocks)
model_router.py (prefer_local routing for internal runtime calls)
api.py (TaskRequest agent_id alias; approved lessons accumulate, dedupe)
tests/conftest.py (JARVIS_AUTO_VERIFY=0 guard)
tests/test_tool_loop_history.py (new)
tests/test_retry_evidence.py (new)
tests/test_lessons_and_verdicts.py (new)
tests/test_agent_local_routing.py (new)
tests/test_agent_collaboration.py (new)
tests/test_stats_agents_endpoint.py (new)
scripts/live_agent_run.py (new)
scripts/run_api.sh (new)
```
Depends on: Commit 13

### Commit 21: Voice — skip zero-sample-rate input devices
```
voice.py (PaMacCore AUHAL guard: exclude devices with defaultSampleRate <= 0)
```

### Commit 22: Test suite stabilization
```
tests/test_jarvis_golden_cases.py (router state reset between cases)
tests/test_jarvis_live_integrations.py (router state reset; Google-auth skipTest; looser agent assertions)
tests/test_jarvis_regression_suite.py (digest fast-path: Email Digest label + local-stream mocks)
```
Depends on: Commit 14 (digest label change)

### Commit 23: Coordination docs (or leave uncommitted by policy)
```
AGENT_BOARD.md
CODEX_CLAUDE_COORDINATION.md
CODEX_HANDOFF.md
```

Open questions for owners: (1) Commit 15 lists `tests/test_phase2_agents.py` which does not exist on disk — drop or write it. (2) `config.py` is already clean — remove it from Commit 15.

## Pre-commit checklist
```bash
# No secrets in staged files
git diff --cached | grep -iE "secret|api_key|password" || echo "clean"

# Full suite
python3 -m pytest tests/ -q --tb=no
# Expected: 1588+ passed, 0 failed

# After ui.py / Jarvis.spec commits:
/Users/truthseeker/jarvis-ai/scripts/install_jarvis_app.sh --applications-only
```
